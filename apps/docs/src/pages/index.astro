---
/*
 * §1 The guide has no landing page of its own: it duplicated the product's
 * own landing page (apps/web/components/LandingContent.tsx) almost word for
 * word. `/guide` now redirects straight to Getting started; the three reader
 * paths (workers / managers / system) are linked directly from the product
 * landing page instead of from a page here.
 */
import { withBase } from '../href'

return Astro.redirect(withBase('/getting-started'))
---
