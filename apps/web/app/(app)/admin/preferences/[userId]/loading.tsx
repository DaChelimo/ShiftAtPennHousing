import { PageSkeleton } from '../../../../../components/ui/PageSkeleton';

// On-behalf preference painter for one worker.
export default function Loading() {
  return <PageSkeleton variant="plain" />;
}
