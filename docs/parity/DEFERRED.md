# Deferred-Items Program — complete list + execution plan

> **⟳ STATUS (2026-06-12, same session):** D1 `1e9a223` · D2-4 `93b27e0` · D5 `95e1649` ·
> D6+D7+D9 `2958db5` · D8 `cba1123` · D10 `7b1a0db` — ALL BUILD ITEMS DONE (mobile JVM
> suite 246→275 green; iOS link green; D1 adb deep-link spot-check PASS). D11/D12 in
> flight: TB specs + Maestro + Playwright runs — results recorded in STATUS.md.

Compiled 2026-06-12 from HANDOFF.md / STATUS.md / PLAN.md / MATRIX.md after all four
parity tracks completed. This is the FULL deferred inventory (the chat summary listed
only D1/D2-4/D5/D10); each row says what unblocks it now and how it gates.

## Inventory

### Build — mobile

| ID  | Item                                                                                                                                                                                                                                                                                                                   | Was deferred because → unblocked by                                                                                                                                     | Gate                                                         |
| --- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------ |
| D1  | **T2-13** Full-screen push-launched FloatAckSurface + deep-link route (`pennshift://float-ack/{floatId}`): Android intent filter + FCM-tap pending intent; iOS `onOpenURL` + push userInfo; pure `FloatDeepLink` parser. FCM creds stay deploy-config — the route is verifiable via `adb` deep link.                   | "reachable only via FCM push, untestable locally" → the DEEP LINK is locally testable (adb `am start -d`); only the push _transport_ stays deploy-config.               | JVM + assemble + iOS link + adb deep-link spot-check         |
| D2  | **T3a-2** Initiate temporary shift swap: propose from a My-Shifts card → pick the counterparty's same-house shift (the T3b house grid is the picker data) → `create-swap` EF (`shift_swap`).                                                                                                                           | "minimal slice only" locked decision → user now asked for the rest; counterparty discovery existed nowhere → T3b `house_schedule_grid` + `worker_directory` provide it. | JVM + assemble + iOS link                                    |
| D3  | **T3a-3** Float swap initiate (worker holding a float proposes a same-house replacement; EF `float_swap`) + permanent swap initiate (recurring slot, `permanent_swap` + `recurring_pattern`).                                                                                                                          | same as D2                                                                                                                                                              | JVM + assemble + iOS link                                    |
| D4  | **T3a-4** Void/cancel own outgoing pending swaps (`void-swap` EF; list own initiator-pending rows) + live update (acceptance already reconciles via the existing `shift_block_assignments` Realtime refetch).                                                                                                          | same as D2                                                                                                                                                              | JVM + assemble + iOS link                                    |
| D5  | **Calendar advanced**: week-picker sheet (quick weeks: last/this/next/+2/+3 + an any-week month grid) over the T3b-4 anchor builders, and the recurring-template view as an honestly-labelled **derived** typical week (union of SCHEDULED-kind slots by dow+time; no template entity exists — banner says "derived"). | "no arbitrary-week data" → REFUTED in T3b-4 (`worker_my_shifts` is date-unbounded; builders take an anchor). Template entity still absent → derive read-only.           | JVM + assemble + iOS link                                    |
| D6  | **Break-pool live feed** (T2-2 residual): wire the break claim pool to live vacant break-period rows (today demo-backed) + wire the 40h meter to the `break-claim` EF hours projection (MATRIX §4.4 row: EF returns current/projected hours, app ignores them).                                                        | "larger wiring, deferred" → just engineering time.                                                                                                                      | JVM + assemble + iOS link                                    |
| D7  | **Urgent-row countdown** (T2-8 residual): the pending-float Updates row shows the live T-10m ack countdown (reuse `FloatAckPresentation` countdown logic).                                                                                                                                                             | "skipped" in T2-8 → small.                                                                                                                                              | JVM + assemble + iOS link                                    |
| D8  | **iOS live week read parity**: `ShiftsObservable`/`CalendarObservable` still render the DEMO snapshot on the live path (writes are live, reads aren't). Activate live reads on iOS like Android's `LiveShiftsRoot`.                                                                                                    | noted in MATRIX ("iOS is agenda-parity but demo-only"); never scheduled.                                                                                                | iOS link (Swift not locally compilable — API-surface verify) |
| D9  | **Preferences deadline-expired lock** (MATRIX §4.1/§4.2 flagged deviation): a worker who never submitted still sees an editable grid after the deadline; the RPC rejects silently. Show the read-only/deadline-passed state client-side (pure: `deadline < now`).                                                      | flagged `5·dev` in the audit, never scheduled.                                                                                                                          | JVM + assemble + iOS link                                    |

### Build — web

| ID  | Item                                                                                                                                                                                                                                                                                                                                                                                  | Notes                                                                                                                                                          | Gate                      |
| --- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------- |
| D10 | **§6.12 integration health cards**: per-integration cards driven by REAL signals where one exists — Push delivery (pending_notification_deliveries backlog/oldest-pending + push_tokens count), Orchestrator (existing row, kept) — and explicit **Not configured** cards for SMS / Allied / SSO / SIS (no integration exists to instrument; replaces the current inline disclosure). | MATRIX: the design brief itself omits these cards (expectation-only gap) — so real-signal + honest not-configured is the correct scope, NOT fake green checks. | type-check + lint + build |

### No-build (documented decisions — do NOT implement)

- **T2-9 notification-channels backing**: the only spec-permitted opt-out channel is broadcast, already backed by `users.broadcast_subscribed` + the `users-broadcast-subscription` EF (T1-7). A per-channel table would back nothing. Marked satisfied.
- **Mobile per-category personal-notification toggles**: locked decision #2 (§10.1 — personal notifications are mandatory/non-silenceable). Stay disabled.
- **Fake-data integration cards**: per D10, SMS/Allied/SSO/SIS render "Not configured", never synthetic health.

### Tests + verification debts

| ID  | Item                                                                                                                                                                                                                         | Gate                                                  |
| --- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------- |
| D11 | **TB backfill** TB-1 web calendar grid (incl. `calendar-closed-day`) · TB-2 hours report · TB-3 coverage perm-openings · TB-4 config+health · TB-5 inbox/force-trigger/leave/rotor/cap/prefs residual · TB-6 mobile residual | Playwright (needs `supabase db reset`) / Vitest / JVM |
| D12 | **Verification runs**: Maestro pass on emulator (worker flows incl. new selectors) + full Playwright run.                                                                                                                    | manual run, recorded in STATUS                        |

## Execution order + rules

D1 → D2-4 (one swaps chunk) → D5 → D6+D7+D9 (small-mobile batch) → D8 → D10 (web; may run as a
subagent while mobile work proceeds — file-disjoint, NO DB) → D11 → D12. Mobile chunks in the
MAIN thread (subagent stall history); ONE DB-holding activity at a time (Playwright/db reset
never alongside emulator). Commit per chunk; STATUS.md row updates last.
