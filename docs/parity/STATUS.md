# Parity Status — live chunk tracker

Update the row (status / gate / commit) as the **last step** of every chunk. See [`PLAN.md`](PLAN.md).

Status legend: ☐ pending · ◐ in-progress · ☑ done (gate green) · ⚠ blocked/needs-decision

> **▶ RESUMING? Read [`HANDOFF.md`](HANDOFF.md) first** — current state, locked decisions (incl. the
> coalescing-build + swaps-minimal-slice rulings), exact remaining work in order, and the load-bearing gotchas.

## Track T1 — Wire existing backends + test

| ID    | Chunk                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | Status | Gate result                     | Commit    |
| ----- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------ | ------------------------------- | --------- |
| T1-0  | Mobile data-layer write foundation (reusable `EdgeFunctionClient`) — created in T1-2; `PreferencesRepository.submitPreferences` refactored onto it (behavior-preserving)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | ☑      | JVM+assemble+iOS green          | `T1-2`    |
| T1-1  | Updates feed live (`fetchNotifications`/`observeNotifications` already exist, just uncalled)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | ☑      | JVM+assemble+iOS green          | `0e7f07a` |
| T1-2  | My Shifts **drop** wired (`drop-shift` / `permanent-drop`, Android + iOS) + creates `EdgeFunctionClient`. Reclaim deferred to T1-3.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | ☑      | JVM+assemble+iOS green          | `8aa8b10` |
| T1-3  | Open Shifts **claim + reclaim** wired (`claim-shift`, Android + iOS) — reclaim is the same EF on the dropped-still-open block (assignment_id preserved on vacate). T1-2 reclaim residual now **resolved**.                                                                                                                                                                                                                                                                                                                                                                                                                                               | ☑      | JVM+assemble+iOS green          | `6c90e2a` |
| T1-4  | Float **ack/decline** wired + **live pending-float** read. New worker-auth EFs `acknowledge-float` / `decline-float` wrap the service-role-only RPCs (mig 20260528000014); read via own-row `float_assignments` + `worker_my_shifts`. Android + iOS; demo unchanged.                                                                                                                                                                                                                                                                                                                                                                                     | ☑      | JVM+assemble+iOS green          | `902d655` |
| T1-5  | Break **claim/drop** wired. Claim → `break-claim` EF (`{assignment_id, claim_type:'temporary'}`); drop reuses `drop-shift` via a new string-keyed `dropShift(assignmentId)` overload (no break-specific drop RPC). Android + iOS; demo path local-only/unchanged. Break pool stays demo-backed (T2-2).                                                                                                                                                                                                                                                                                                                                                   | ☑      | JVM+assemble+iOS green          | `277999e` |
| T1-6  | Preferences submit                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | ☑      | pre-landed                      | `f53d335` |
| T1-7  | Settings **broadcast toggle** wired (PATCH `users-broadcast-subscription`) + **live profile read** (own `users` + `user_roles` + `houses`, all RLS). New `ProfileRepository` + `EdgeFunctionClient.patch`. Android + iOS; demo path local-only. The three personal-notif rows stay disabled (§10.1).                                                                                                                                                                                                                                                                                                                                                     | ☑      | JVM+assemble+iOS green          | `a5cd480` |
| T1-8  | Login live path. Credential sign-in already live (`LoginReducer`/`LoginFormValidator` + `SupabaseAuthGateway`); SSO folded into the one credential CTA (no SSO backend), keep-signed-in informational. Closed the iOS launch session-restore gap.                                                                                                                                                                                                                                                                                                                                                                                                        | ☑      | JVM 165+assemble+iOS+link green | `75a4581` |
| T1-9  | Web inbox **realtime** wired. `useInboxRealtime` hook in client `ActionInbox` opens a `postgres_changes` channel on `notifications` via the existing browser client (authed cookie session → RLS-scoped to this admin); on any change → `router.refresh()` re-runs the server component + re-partitions via `@shift/core` (no client-side enrich dup). Cleanup via `removeChannel`. **e2e/Playwright deferred** (local DB held).                                                                                                                                                                                                                         | ☑      | type-check+lint+build green     | `15cbfb6` |
| T1-10 | **Surface live pending-float in mobile Updates → ack hero** (NEW — from spot-check). Outcome (A): float-lookup + force-trigger RPCs DO insert a `personal_shift` notification (`payload.kind='float_assigned'`, `payload.float_id`), but the mobile mapping ran it through generic `categoryForType` → non-actionable INFO row (`opensAck=false`). Fixed: pure `notificationFromPayload` reads the payload → urgent FLOAT row that opens the ack hero; `withPendingFloatEntry` synthesizes the urgent entry from live `fetchPendingFloat` if the notification lagged. Android + iOS live roots; Maestro selectors + demo path unchanged. +6 kotlin.test. | ☑      | JVM+assemble+iOS+link green     | `1e8b07e` |

## Track CO — Block-coalescing foundation (gates T2-10/T2-11)

| ID  | Chunk                                                                                                                                                                                                                                                                                                                                                                                                                                 | Status | Gate result                               | Commit    |
| --- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------ | ----------------------------------------- | --------- |
| CO  | **Block coalescing built** (pure `shifts/Coalesce.kt`): contiguous same-(house,kind,§11.2-flags) per-block rows → ONE displayed card carrying `blockIds` (new on `MyShift`/`OpenShift`, default `listOf(id)` = demo unchanged). Applied in `ShiftsScreenViewModel` (3 tabs) + calendar agenda; write paths cover all blocks (drop = one multi-id call, claim/reclaim per-block loop, permanent slot = all NY-local starts). DST-safe. | ☑      | JVM 209 PASS (+23) + assemble+iOS compile | `0650231` |

## Track T2 — Build missing backend + UI

| ID     | Chunk                                                                                                                                                                                                                                                                                                                                                                                                                                              | Status     | Gate result                                  | Commit    |
| ------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------- | -------------------------------------------- | --------- |
| T2-1   | **dropped_still_open** read-model fixed: persist `dropped_by_user_id`+`dropped_at` in `drop_shift`, OR'd own-drop RLS, view computes it; no-clear-on-refill verified vs `claim_open_shift`. Closed-house §3.4/§11.3 split → **T2-12c** (display, deferred).                                                                                                                                                                                        | ☑          | pgTAP 1180 PASS (8 new) + tsc                | `959f1b8` |
| T2-2a  | Break **live context** (`break_periods` worker SELECT RLS; name/window + winter_break→"only Harnwell open" derived live) + **at-cap claim block** (40h, disabled Claim). +18 pgTAP/kotlin.test. Pool live-feed still demo.                                                                                                                                                                                                                         | ☑          | pgTAP 1188 PASS + JVM+assemble+iOS+link      | `b78683e` |
| T2-2b  | **Break "no-break-hours" opt-out** built (mobile). Presence of own `break_optouts` (break_id,user_id) row = opt-out; `BreakRepository` writes directly via Postgrest (no EF/migration); toggle + opted-out empty state both platforms. +6 kotlin.test.                                                                                                                                                                                             | ☑          | JVM+assemble+iOS+link                        | `c227f06` |
| T2-2c  | Break §4.4 **T-1d drop routing** — **already enforced** (phase-driven at the view layer: pool/feed views gate on `status='vacant'` + start-anchored `break_claim_phase`, not `vacancy_origin`). Added 9-test regression `break-drop-t1d-routing.sql`.                                                                                                                                                                                              | ☑          | pgTAP 1233 PASS                              | `6280011` |
| T2-3   | Permanent pickup. **Backend already implemented & green** (matrix "501 stub" was stale; landed `381650d`/`6a3ae64`/`555dad8`; verified 1221 pgTAP + 652 Vitest). **T2-3b** wired mobile flow to the real `permanent-pickup` EF (GET dry-run "N of M weeks · K skipped" + POST), `EdgeFunctionClient.get` added (`1f5e223`). **T2-3c** web permanent-openings feed **already exists** (coverage `permPerDay`+`PermCard`) → untested, moved to TB-3. | ☑          | mobile JVM+assemble+iOS; backend 1221 pgTAP  | `1f5e223` |
| T2-4   | **Both already implemented; added missing tests.** §8.4.1 permanent-drop popup confirmed wired (drop sheet radios → `permanentDrop`/`dropShift`). §5.5 float-drop exception satisfied: `drop_shift` permits all 4 float statuses, orchestrator re-escalates vacated rows independently. Added 3 pgTAP (phase-07-f) + 1 kotlin.test locking it.                                                                                                     | ☑          | pgTAP 1224 + mobile builds green             | `0111e82` |
| T2-5   | **Set preference deadline** (§4.2): `set_preference_deadline` RPC (authz SM/HM/BM, rejects published period / deadline-after-start, NY-anchored) + `set-preference-deadline` EF + web server-action `DeadlineEditor` (enables the prev-disabled control). _2 subagents timed out on the long DB gate → finished in main thread._                                                                                                                   | ☑          | pgTAP 1197 PASS (9 new) + web build clean    | `df419ea` |
| T2-6   | **Hire** built (`hire_worker` RPC + `hire-worker` EF: HM/BM-only, `auth.admin.createUser`+rollback, atomic users+roles, §4.5 no-shifts) + web `HireWorkerControl` (replaces disabled button). **Fire** confirmed already wired (action→`fire_worker` RPC) — no change. +24 pgTAP.                                                                                                                                                                  | ☑          | db reset + test db 1221 PASS + web build     | `63d8266` |
| T2-7   | Rotor academic-year truncation (spec bug)                                                                                                                                                                                                                                                                                                                                                                                                          | ☑          | Vitest 652 + web type-check + lint green     | `8ab4213` |
| T2-8   | **Mark-(all-)read** wired. Backend `mark_notification_read` RPC already existed (granted authenticated); mobile calls it via direct Postgrest (`markNotificationRead`/`markAllRead` loop), `UpdatesViewModel.markAllRead` + "Mark all read" header (`mark_all_read`) on both platforms. +6 kotlin.test. (Urgent-row countdown skipped.)                                                                                                            | ☑          | JVM+assemble+iOS green                       | `e7962d5` |
| T2-9   | Notification channels backing                                                                                                                                                                                                                                                                                                                                                                                                                      | ☐          | —                                            | —         |
| T2-10  | **Partial-claim UI built (§5.3 design-extra):** pure `planPartialClaim`/`subOpenShiftFor` (+NY labels) + ClaimSheet "How much can you cover?" range control on multi-block WEEKLY openings (default whole — Maestro 02 unchanged); meter + soft/hard cap gating recompute from the SELECTED span; confirm claims the effective sub-shift per-block via `claim-shift` (FCFS server-side); untaken blocks re-coalesce open. Permanent = whole slot.  | ☑          | JVM 225 PASS (+7) + assemble + iOS link      | `c2ea19d` |
| T2-11  | **Partial drop UI built (§5.2):** pure `planPartialDrop`/`blockIndexAt`/`subShiftFor` (+NY labels precomputed for Swift) + DropSheet range control (Compose RangeSlider / SwiftUI steppers, defaults whole-shift) + mid-shift "From now" (§5.2 17:51→17:30 pinned); selected run → one `drop-shift` call, rest re-coalesces. DemoData now per-block (live shape; visuals identical). New `drop_range_*`/`drop_from_now` selectors.                 | ☑          | JVM 218 PASS (+9) + assemble + iOS link      | `dd5846d` |
| T2-12a | Builder Phase-2 worker search (§6.2); house switcher (§6.1) was already shipped (S6).                                                                                                                                                                                                                                                                                                                                                              | ☑          | web build clean                              | `ec9fd06` |
| T2-12b | **Leave §2.6 correctness done:** dual-HM/BM coverage guard (#162) + depth-10 resolution-walk flag/notify (#148) built; HMOD-interval transfer (#136-138) already correct (pinned). mig 20260611000006, +21 pgTAP. **Integration health cards (§6.12 SMS/Allied/SSO/SIS) DEFERRED** — no real integration source to instrument (flag-forever).                                                                                                      | ☑          | db reset + test db 1265 PASS                 | `84b348b` |
| T2-12c | Closed-house display (§3.4/§11.3) **COMPLETE**: **be** `house_closure(text,date)` (`d17049b`); **web** "Closed" cells (`1f7706c`); **mobile** muted strip cell + "Closed" chip + "House closed" empty state, `fetchCalendarClosedDays` (own `home_house_id` + per-date RPC, best-effort) → `CalendarViewModel(closedDayIndexes)`; cross-house pickups still render on a closed date. Selectors `calendar_closed_day`/`calendar_closed_chip`.       | ☑          | be 1244 pgTAP; web build; JVM 228 + iOS link | `ecfb458` |
| T2-13  | Full-screen push-launched FloatAckSurface + deep-link route. **DEFERRED** (low value): the full-screen variant is reachable ONLY via FCM push, which is deploy-config (no `google-services.json` committed) and can't be exercised locally; the bottom-sheet ack already covers the in-app flow (T1-10). Revisit when FCM is wired at deploy. (2 subagent stream-stalls + a broken Compose `weight` import; reverted to green.)                    | ⚠ deferred | —                                            | —         |

## Track T3a — Swaps on mobile

| ID    | Chunk                                                                                                                                                                                                                                                                                                                                                                                                                                                   | Status     | Gate result                             | Commit    |
| ----- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------- | --------------------------------------- | --------- |
| T3a-1 | **Swap data layer + accept/reject from feed built (minimal slice per locked decision #4):** `fetchIncomingSwaps` (own pending counterparty `swap_requests`, RLS) + pure `withIncomingSwapEntries` feed synthesis (create-swap writes no counterparty notification) + Accept/Decline actions → `accept-swap`/`reject-swap` EFs + optimistic `resolveSwap`. Permanent swaps = Decline-only (accept needs §8.4 affected ids). Both platforms + demo entry. | ☑          | JVM 234 PASS (+6) + assemble + iOS link | `952baf7` |
| T3a-2 | Initiate temporary shift swap — **DEFERRED** (locked decision #4: accept/reject slice only)                                                                                                                                                                                                                                                                                                                                                             | ⚠ deferred | —                                       | —         |
| T3a-3 | Float swap + permanent swap initiate — **DEFERRED** (locked decision #4)                                                                                                                                                                                                                                                                                                                                                                                | ⚠ deferred | —                                       | —         |
| T3a-4 | Void/cancel + calendar live-update — **DEFERRED** (locked decision #4)                                                                                                                                                                                                                                                                                                                                                                                  | ⚠ deferred | —                                       | —         |

## Track T3b — Contact / grid / calendar-advanced (UNBLOCKED 2026-06-12: user ruled FULL DIRECTORY)

| ID    | Chunk                                                                                                                                                                                                                                                                                                                                                                         | Status | Gate result                            | Commit    |
| ----- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------ | -------------------------------------- | --------- |
| T3b-1 | **Backend built per the FULL-DIRECTORY ruling:** `worker_directory` owner-rights view (name/phone only; REVOKE+SELECT-only — first pgTAP run caught a real write-through-the-view RLS bypass via Supabase default privileges, now pinned) + `houses.desk_phone` + `house_schedule_grid` (security_invoker, home-house scoped). Date-param model unnecessary (view unbounded). | ☑      | db reset + test db **1280 PASS** (+15) | `2e7820f` |
| T3b-2 | Contact-lookup sheet (worker name/phone + Call via tel:/ACTION_DIAL + desk fallback) — built with T3b-3                                                                                                                                                                                                                                                                       | ☑      | JVM 241 + assemble + iOS link          | `39ce2b4` |
| T3b-3 | House schedule tab (`tab_house`): week strip + per-day coalesced roster (pending-float/float-in tags, vacant gaps, active marker) over `fetchHouseSchedule`; live both hosts + demo snapshot                                                                                                                                                                                  | ☑      | JVM 241 (+7) + assemble + iOS link     | `39ce2b4` |
| T3b-4 | Calendar **week navigation** (prev/next chevrons; anchor-param builders, DST-safe `shiftWeekAnchor`; date-based today/NOW-line; closed-days current-week only). Month + permanent-template OUT (no template entity). Tab-indicator polish `6b01c49`.                                                                                                                          | ☑      | JVM 246 (+5) + assemble + iOS link     | `36f5779` |

## Track TB — Test backfill

| ID   | Chunk                                                  | Status | Gate result | Commit |
| ---- | ------------------------------------------------------ | ------ | ----------- | ------ |
| TB-1 | Web live-calendar grid                                 | ☐      | —           | —      |
| TB-2 | Web hours report                                       | ☐      | —           | —      |
| TB-3 | Web coverage monitor                                   | ☐      | —           | —      |
| TB-4 | Web config + health                                    | ☐      | —           | —      |
| TB-5 | Web inbox/force-trigger/leave/rotor/cap/prefs residual | ☐      | —           | —      |
| TB-6 | Mobile residual                                        | ☐      | —           | —      |

---

## Verification log

- **2026-06-12 spot-check (demo path, Pixel 9 emulator) — all new surfaces PASS.**
  - **Coalescing (CO):** per-block DemoData renders as single merged cards on My-Shifts /
    Open-Shifts / Calendar (visuals identical to the pre-CO hand-built spans).
  - **Partial drop (T2-11):** DropSheet range selector (default whole) → "Drop 14:30 – 15:30"
    → the hour lands in Dropped—still-open (strikethrough + Reclaim) and the 13:30–14:30
    remainder re-coalesces into its own Scheduled card.
  - **Partial claim (T2-10):** ClaimSheet "How much can you cover?" → 1h selection recomputes
    the meter (10h→9h of 20h) → claim_success toast; the untaken 14:30–15:30 hour stays
    claimable in the weekly feed.
  - **Swap entry (T3a-min):** Updates shows "Swap request — Shift swap" with Action-needed +
    Accept/Decline; Decline resolves the entry from the feed.
  - One defect found+fixed (`b116f41`): the half-width claim confirm truncated the range
    label → now "Claim {duration}". Emulator killed after the pass.

- **2026-06-11 spot-check (live emulator + local Supabase) — both round-trips PASS.**
  - **Claim** — live UI: login as `alice-quad@upenn.edu` -> claim a DuBois shift -> confirm; DB `shift_block_assignments` flipped `NULL/vacant` -> `alice-quad/claimed`. Mobile->`claim-shift` EF->`claim_open_shift` RPC->DB proven.
  - **Float ack** — EF-with-real-JWT (the exact call the app issues; the live Updates feed did NOT surface the float — see T1-10): `float_assignments.status` `pending`->`acknowledged`, source/dest blocks updated, idempotent re-ack returns `not_pending`. DB restored after.

### Local-dev gotchas (from the spot-check)

- **After adding/changing an Edge Function, recreate the edge-runtime container**: `supabase stop && supabase start` (NOT just `docker restart`). The edge runtime bakes `SUPABASE_INTERNAL_FUNCTIONS_CONFIG` at provision time, so EFs added after the container was created return **404 'Function not found'** until recreated (DB volume preserved). This is why `acknowledge-float`/`decline-float` (T1-4) 404'd locally.
- **Active seed is `supabase/seeds/manual-test.sql`**, NOT the default. Creds for the claimable/scheduled population: `<first>-<house>@upenn.edu` / **`abc123`** (e.g. `alice-quad@upenn.edu`). The `@pennhousing.test` users are hidden by it and have no open shifts/floats.
- **A web chunk that consumes a NEW `database.types.ts` entry (e.g. a new `.rpc('fn')`) must rebuild the shared package: `pnpm --filter @shift/shared build`.** `@shift/shared` exports from a **gitignored built `dist/`** (`main`/`types` → `./dist/…`), so regenerating `packages/shared/src/database.types.ts` alone leaves the web `tsc` resolving the stale `dist` (`error TS2345: "fn" not assignable to <Functions union>`). Sequence for any DB+web chunk: `supabase db reset` → `supabase gen types … > …/database.types.ts` → `pnpm --filter @shift/shared build` → web `type-check`/`build`.
- **Long DB gates (`supabase db reset && supabase test db`) can idle-timeout a subagent's stream** (watchdog killed two T2-5 subagents). Run these from the orchestrator (main thread) via `run_in_background`, or split the chunk so the subagent isn't holding an idle stream through a multi-minute reset.

### Parallel-execution rule (two streams)

The local Supabase DB is a SHARED resource: **at most ONE DB-migration/reset chunk in flight at a time** (`supabase db reset`/`test db`/migration-apply). Fill the other parallel slot with a NO-DB chunk (web UI build-gated, mobile UI compile-gated, `packages/core` Vitest). Never run an emulator spot-check or Playwright concurrently with a DB-reset chunk.

- **KILL the emulator after a spot-check** (`adb -s emulator-5554 emu kill`). A leftover running emulator silently ballooned a normally-16s mobile Gradle build to **21min** (and OOM-failed it). If `:androidApp:assembleDebug` is suddenly minutes-long, check `adb devices` first.

_Last updated (HEAD `6b01c49`): **ALL TRACKS COMPLETE.** T1 + T2 + CO/partials + T3a-min as before; **T3b UNBLOCKED and built 2026-06-12** after the user ruled cross-worker contact = **FULL DIRECTORY**: T3b-1 backend (pgTAP 1265→**1280**; the suite's first run caught and pinned a real write-through-view RLS bypass from Supabase default privileges) + T3b-2/3 House tab + contact sheet + T3b-4 calendar week navigation (mobile JVM suite → **246**, iOS link green; second emulator spot-check verified House/contact/week-nav and caught the tab-indicator constant bug, fixed `6b01c49`). Still deferred by design: T2-13 (push-only), integration health cards (no source), T3a-2..4 initiate/void (locked decision #4), calendar month/template views (no template entity). Optional remaining: TB test backfill + Maestro/Playwright verification debts. NOTE: `database.types.ts` NOT regenerated for the T3b views (the file carries a teammate's uncommitted dev-sim-clock work; the new views aren't TS-consumed — regen with their next migration cycle). Emulator killed; only the teammate's dev-clock files remain uncommitted._
