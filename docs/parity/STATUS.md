# Parity Status — live chunk tracker

Update the row (status / gate / commit) as the **last step** of every chunk. See [`PLAN.md`](PLAN.md).

Status legend: ☐ pending · ◐ in-progress · ☑ done (gate green) · ⚠ blocked/needs-decision

## Track T1 — Wire existing backends + test

| ID   | Chunk                                                                                                                                                                                                                                                                                                  | Status | Gate result                     | Commit    |
| ---- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------ | ------------------------------- | --------- |
| T1-0 | Mobile data-layer write foundation (reusable `EdgeFunctionClient`) — created in T1-2; `PreferencesRepository.submitPreferences` refactored onto it (behavior-preserving)                                                                                                                               | ☑      | JVM+assemble+iOS green          | `T1-2`    |
| T1-1 | Updates feed live (`fetchNotifications`/`observeNotifications` already exist, just uncalled)                                                                                                                                                                                                           | ☑      | JVM+assemble+iOS green          | `0e7f07a` |
| T1-2 | My Shifts **drop** wired (`drop-shift` / `permanent-drop`, Android + iOS) + creates `EdgeFunctionClient`. Reclaim deferred to T1-3.                                                                                                                                                                    | ☑      | JVM+assemble+iOS green          | `8aa8b10` |
| T1-3 | Open Shifts **claim + reclaim** wired (`claim-shift`, Android + iOS) — reclaim is the same EF on the dropped-still-open block (assignment_id preserved on vacate). T1-2 reclaim residual now **resolved**.                                                                                             | ☑      | JVM+assemble+iOS green          | `6c90e2a` |
| T1-4 | Float **ack/decline** wired + **live pending-float** read. New worker-auth EFs `acknowledge-float` / `decline-float` wrap the service-role-only RPCs (mig 20260528000014); read via own-row `float_assignments` + `worker_my_shifts`. Android + iOS; demo unchanged.                                   | ☑      | JVM+assemble+iOS green          | `902d655` |
| T1-5 | Break **claim/drop** wired. Claim → `break-claim` EF (`{assignment_id, claim_type:'temporary'}`); drop reuses `drop-shift` via a new string-keyed `dropShift(assignmentId)` overload (no break-specific drop RPC). Android + iOS; demo path local-only/unchanged. Break pool stays demo-backed (T2-2). | ☑      | JVM+assemble+iOS green          | `277999e` |
| T1-6 | Preferences submit                                                                                                                                                                                                                                                                                     | ☑      | pre-landed                      | `f53d335` |
| T1-7 | Settings **broadcast toggle** wired (PATCH `users-broadcast-subscription`) + **live profile read** (own `users` + `user_roles` + `houses`, all RLS). New `ProfileRepository` + `EdgeFunctionClient.patch`. Android + iOS; demo path local-only. The three personal-notif rows stay disabled (§10.1).   | ☑      | JVM+assemble+iOS green          | `a5cd480` |
| T1-8 | Login live path. Credential sign-in already live (`LoginReducer`/`LoginFormValidator` + `SupabaseAuthGateway`); SSO folded into the one credential CTA (no SSO backend), keep-signed-in informational. Closed the iOS launch session-restore gap.                                                      | ☑      | JVM 165+assemble+iOS+link green | `75a4581` |
| T1-9 | Web inbox realtime                                                                                                                                                                                                                                                                                     | ☐      | —                               | —         |

## Track T2 — Build missing backend + UI

| ID    | Chunk                                                                   | Status | Gate result | Commit |
| ----- | ----------------------------------------------------------------------- | ------ | ----------- | ------ |
| T2-1  | Read-model fixes (dropped_still_open, closed-house)                     | ☐      | —           | —      |
| T2-2  | Break completeness (periods, no-hours opt-out, T-1d routing)            | ☐      | —           | —      |
| T2-3  | Permanent pickup (backend 501→real + UI + web feed)                     | ☐      | —           | —      |
| T2-4  | Worker permanent drop + float-drop exception                            | ☐      | —           | —      |
| T2-5  | Set-deadline RPC + web wire                                             | ☐      | —           | —      |
| T2-6  | Hire/Fire RPC + web People                                              | ☐      | —           | —      |
| T2-7  | Rotor academic-year truncation (spec bug)                               | ☐      | —           | —      |
| T2-8  | Mark-read (UPDATE policy + UI)                                          | ☐      | —           | —      |
| T2-9  | Notification channels backing                                           | ☐      | —           | —      |
| T2-10 | Partial-claim (design-extra)                                            | ☐      | —           | —      |
| T2-11 | Partial drop UI (§5.2)                                                  | ☐      | —           | —      |
| T2-12 | Web build-missing (switcher, closed-house, search, leave, config cards) | ☐      | —           | —      |
| T2-13 | Full-screen FloatAckSurface + routing                                   | ☐      | —           | —      |

## Track T3a — Swaps on mobile

| ID    | Chunk                                     | Status | Gate result | Commit |
| ----- | ----------------------------------------- | ------ | ----------- | ------ |
| T3a-1 | Swap data layer + accept/reject from feed | ☐      | —           | —      |
| T3a-2 | Initiate temporary shift swap             | ☐      | —           | —      |
| T3a-3 | Float swap + permanent swap initiate      | ☐      | —           | —      |
| T3a-4 | Void/cancel + calendar live-update        | ☐      | —           | —      |

## Track T3b — Contact / grid / calendar-advanced

| ID    | Chunk                                                                                         | Status | Gate result | Commit |
| ----- | --------------------------------------------------------------------------------------------- | ------ | ----------- | ------ |
| T3b-1 | ⚠ Backend (desk-phone + cross-worker RLS + roster view + date-param model) — **RLS decision** | ⚠      | —           | —      |
| T3b-2 | Shift-detail + contact-lookup sheet                                                           | ☐      | —           | —      |
| T3b-3 | House schedule grid                                                                           | ☐      | —           | —      |
| T3b-4 | Calendar advanced (week-picker, month, template)                                              | ☐      | —           | —      |

## Track TB — Test backfill

| ID   | Chunk                                                  | Status | Gate result | Commit |
| ---- | ------------------------------------------------------ | ------ | ----------- | ------ |
| TB-1 | Web live-calendar grid                                 | ☐      | —           | —      |
| TB-2 | Web hours report                                       | ☐      | —           | —      |
| TB-3 | Web coverage monitor                                   | ☐      | —           | —      |
| TB-4 | Web config + health                                    | ☐      | —           | —      |
| TB-5 | Web inbox/force-trigger/leave/rotor/cap/prefs residual | ☐      | —           | —      |
| TB-6 | Mobile residual                                        | ☐      | —           | —      |

---

_Last updated: T1-8 done (**Login live path** verified + the one real gap — iOS launch session-restore — closed; code `75a4581`). **Credential sign-in was already live end-to-end** on both platforms: the pure `LoginReducer` / `LoginFormValidator` drive the form, `SupabaseAuthGateway.signIn` (GoTrue email+password) authenticates, and a non-null `currentSession()` read-back wires the worker JWT (`WorkerBackend.wireAccessToken`) and routes to live shifts — no demo bypass on the LIVE path (Android `MainActivity.LiveOrLoginRoot`, iOS `LiveRootView`/`LoginObservable`). Sign-out (`AuthGateway.signOut`, wired in T1-7) confirmed on both. **Two design-deviations resolved as intended (no code beyond comments):** the design's "Sign in with PennKey" SSO is correctly FOLDED into the single credential CTA — there is NO SSO backend (the gateway is email+password) and the "Sign in with PennKey" button already submits the credential form, so no separate/unimplemented redirect path exists; left flagged in the screen header comments. "Keep me signed in" is INFORMATIONAL only — Supabase persists the session via storage regardless, so the checkbox toggles a harmless local boolean (it does not gate restore); left as-is. **The one real gap (iOS launch session-restore) is now closed:** iOS `LiveRootView` previously went straight to login and only promoted to shifts after a FRESH sign-in, whereas Android `MainActivity` restored any persisted session on launch via `produceState { authGateway.currentSession() }` + `AppBootstrap.decide`. Added `WorkerBackend.restoreValidSession()` (data-layer, NOT the pure `auth/` package): it restores the persisted Supabase session through the gateway (`loadFromStorage` + `awaitInitialization` + `currentSessionOrNull`), applies the shared/tested `SessionValidity` gate Kotlin-side — so the Swift caller never bridges `kotlin.time.Instant`/`Duration` — wires the worker JWT on a valid restore, and returns the valid session or null. iOS `LiveRootView` calls it once on cold launch (`.task`, an `AuthSession??` restore-state with a brief `LaunchRestoreView` ProgressView while in flight); a valid restore routes straight to `ShiftsRootView`, a sign-out forces login, an in-session sign-in still takes precedence. The pure `auth/` reducer/validator + their 165 JVM tests are untouched; no tests added (gateway/data/UI layer is untested by design). Gate (`apps/mobile`): `:shared:testAndroidHostTest` 165/165, `:androidApp:assembleDebug`, `:shared:compileKotlinIosSimulatorArm64`, plus `:shared:linkDebugFrameworkIosSimulatorArm64` (SKIE Swift export of `restoreValidSession`) all green. Previously — T1-7 done (Settings **broadcast toggle** + **live profile read** wired, Android + iOS; demo path unchanged local-only; code `a5cd480`). The broadcast EF is **PATCH-only** at `/users/{userId}/broadcast_subscribed` with body `{ broadcast_subscribed: <bool> }` (it 405s on anything but PATCH and 403s an HM/BM subscribe), so `EdgeFunctionClient` gained a `patch(path, jsonBody)` sibling to `invoke` (same auth/headers) and `ProfileRepository.setBroadcastSubscription(userId, subscribed)` PATCHes it. The **profile join** is three RLS-scoped reads mapped into the pure `settings/SettingsProfile`: own `users` row (`name`, `email`, `home_house_id`, `broadcast_subscribed` — "users can select own row"), own `user_roles` (highest of bm>hm>sm>sw, default sw — "users can select own roles"), and `houses` (home-house `name`, authenticated read added in migration 20260605000001; falls back to the id if the join row is missing). New `WorkerBackend.profileRepository` + `DemoFactory.settingsViewModel(snapshot:)` live factory. Android `MainActivity.LiveShiftsRoot` builds the `SettingsViewModel` from `produceState`-loaded `fetchProfile` (demo fallback while loading) and injects `onToggleBroadcast` (best-effort PATCH on the live path); iOS adds `SettingsObservable.activateLive` + `SettingsScreen.onToggleBroadcast`, wired in `ContentView` (live only, via `.task`). The optimistic local toggle still flips first (mirrors claim/drop); the new subscription value is read back synchronously from the VM and POSTed. **§10.1 flagged exception honored:** only GENERAL_UPDATES is interactive — the FLOAT / SHIFT_REMINDERS / SCHEDULE_PUBLISHED rows keep `interactive=false` (Compose `enabled=false`, SwiftUI `.disabled(true)`), untouched, so personal notifications stay mandatory/non-silenceable. Pure `settings/` + `SettingsViewModel` internals untouched; no tests added (data/UI layer). Gate: JVM host 165/165, `:androidApp:assembleDebug`, `:shared:compileKotlinIosSimulatorArm64` all green. Previously — T1-5 done (break **claim/drop** wired, Android + iOS; demo path unchanged optimistic-local; code `277999e`). The `break-claim` EF request shape is IDENTICAL to `claim-shift` — `{ assignment_id: <uuid>, claim_type: 'temporary' }` (the EF rejects any non-`'temporary'` claim_type) — so `claimBreak(assignmentId)` reuses the existing `ClaimShiftRequest` serializer against the `break-claim` path. **Break drop cleanly reused `dropShift`:** added a string-keyed `dropShift(assignmentId)` overload (the `MyShift` overload now delegates to it); a claimed break shift's `worker_my_shifts` row id IS its block `assignment_id`, exactly what the generic `drop-shift` EF keys on — no break-specific drop RPC exists or is needed (confirmed in `break-claim/index.ts` + the `BreakClaim.kt` GAP note). UI wiring mirrors T1-2/T1-3/T1-4: `BreakClaimScreen` (Compose) / `BreakClaimView` (SwiftUI) take optional `onClaim`/`onDrop` callbacks invoked alongside the optimistic VM `claim`/`drop`; `ShiftsApp` threads `onClaimBreak`/`onDropBreak`; `MainActivity.LiveShiftsRoot` (Android) and `ContentView`'s `.breakShifts` case (iOS) inject best-effort POSTs on the LIVE path only (demo passes nil/default = local-only, unchanged). **Server stays authoritative** for the 40h break HARD cap (BSpec §4) and the Harnwell training constraint (invariant #1 — no non-Harnwell worker may claim a Harnwell break shift); the client meter/gating is a pre-check only. Pure `breakclaim/` + `BreakClaimViewModel` internals untouched; no tests added (data/UI layer). **T2-2 residuals confirmed (left as-is):** the break pool/VM stays demo-backed (no `break_periods` worker SELECT, so the break NAME / "only Harnwell open" context copy is still caller-supplied), the §4.4 "no-break-hours" opt-out control, the §4.4 T-1d drop-routing enforcement (shown as descriptive meta only today), and the at-cap claim-block UI. **Previously — T1-4 done** (float **ack/decline** wired live + **live pending-float** read, Android + iOS; demo path unchanged optimistic-local). **Backend gap closed, not blocked:** the `acknowledge_float` / `decline_float` RPCs (mig 20260528000014) are `GRANT EXECUTE … TO service_role` ONLY — a worker's JWT cannot call them through PostgREST — so two NEW thin worker-authenticated EFs `acknowledge-float` / `decline-float` wrap them (mirror `claim-shift`: auth the bearer, then `rpc(..., p_user_id := user.id)` with the service-role key; the worker's own ack/decline is the legitimate manual action under no-takeback invariant #3). EFs pass the RPC's idempotent terminal result (`{acknowledged|declined:false, reason:'not_pending'}`) through as 200. **Live read wired (not blocked):** `WorkerShiftsRepository.fetchPendingFloat(userId)` reads the worker's own `status='pending'` `float_assignments` row (own-row SELECT policy `user_id=auth.uid()`) for `float_id`+`destination_assignment_ids`, then anchors destination house + float start from the worker's own pending float-out blocks (`worker_my_shifts`) — both RLS-scoped, no new view/RLS needed. `MainActivity.LiveShiftsRoot` / iOS `AckHostObservable` build the ack VM from it (fall back to demo float while loading / when none). Ack/decline POST best-effort ONLY when the optimistic local VM transition succeeds (no POST on a past-deadline no-op tap). Updates urgent-row→ack linkage deferred (T1-1 carry / T2-13). Pure `ack/` + VM internals untouched; no tests added. Previously — T1-3 done (Open-Shifts claim wired live to `claim-shift`, Android + iOS; demo path unchanged optimistic-local). Reused `EdgeFunctionClient`; added `WorkerShiftsRepository.claimShift(OpenShift)` + `reclaimShift(MyShift)`, both POSTing `{assignment_id, claim_type:'temporary'}`. Server stays authoritative for hours-cap / T-2h cutoff / cross-house (Harnwell invariant #1) / FCFS; client cap+claimable gating is a pre-check only. **Reclaim residual (from T1-2) RESOLVED**: `drop_shift` vacates the block in place (status→'vacant') WITHOUT changing its `assignment_id`, so a dropped-still-open `MyShift.id` IS the vacant `worker_open_shifts.id` — reclaim is just `claim-shift` on that id (no separate backend exists or is needed). Permanent pickup stays out of scope (`claim-shift` returns 501 for `claim_type:'permanent'` → T2-3). Residual unchanged: week-total hours chip still demo, `dropped_still_open` read-model (→ T2-1). Next: T1-4 (float ack/decline) reuses the RPC path._
